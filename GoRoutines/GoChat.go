package main

import (
	"bufio"	
	/*Package bufio implements buffered I/O. 
	It wraps an io.Reader or io.Writer object, creating another object 
	(Reader or Writer) that also implements the interface but provides	 
	buffering and some help for textual I/O. */
	"net"
	/*Package net provides a portable interface for network I/O, 
	including TCP/IP, UDP, domain name resolution, and Unix domain 
	sockets.*/
)


// need client structure
	// client needs to read, listen, write
	// method for creating new clients

// need server structure
	// need to add clients to server	
	// need to listen to input from clients
	// need to relay input to all clients
	// method for creating new server
	// main that is infinite loop of listening and relaying


